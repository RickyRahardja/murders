We analyze US gun murder data collected by the FBI.

download-data.R downloads csv file to data directory.

wrangle-data.R creates a derived dataset and save an object in rda directory.

analysis.R generate plot and save it to the figs directory.
